{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "data-ingest:contract/contract.schema.json",
    "title": "contract",
    "description": "A data contract within data ingest. This specifies the information required to model and read the data",
    "type": "object",
    "properties": {
        "types": {
            "description": "Predefined types to be used within schema/dataset definitions",
            "type": "object",
            "additionalProperties": {
                "$ref": "components/field_specification.schema.json"
            }
        },
        "schemas": {
            "description": "Predefined models to be used within dataset definitions",
            "type": "object",
            "additionalProperties": {
                "$ref": "components/entity.schema.json"
            }
        },
        "datasets": {
            "description": "Models which represent entities within the data",
            "type": "object",
            "additionalProperties": {
                "$ref": "components/readable_entity.schema.json"
            },
            "minProperties": 1
        },
        "error_details": {
            "description": "Relative path from the dischema document to reference an error details document.",
            "type": "string",
            "pattern": "^[a-zA-Z0-9].*_data_contract_error_details\\.json$"
        }
    },
    "required": ["datasets"],
    "additionalProperties": true
}
